import java.util.List;

public interface Sorting<T extends Comparable<T>> {
    void sort(List<T> nums);
    /*
    as you (person who created task, not exactly you) it just interface
    it just serve to declare method
    */
}